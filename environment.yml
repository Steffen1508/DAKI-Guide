name: aau-ai
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - jupyter
  - ipykernel
  - seaborn
  - pip
  # PyTorch kan tilføjes senere når I skal lave deep learning:
  # se https://pytorch.org for den rigtige kommando til jeres system
